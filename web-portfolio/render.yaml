services:
  - type: web
    name: portfolio
    env: node
    buildCommand: |
      cd web-portfolio
      npm install --legacy-peer-deps
      npm run build
    staticPublishPath: ./web-portfolio/dist
    envVars:
      - key: NODE_VERSION
        value: 18.18.0
    autoDeploy: true